# redux-thunk-store

This library was generated with [Nx](https://nx.dev).

## Running unit tests

Run `nx test redux-thunk-store` to execute the unit tests via [Jest](https://jestjs.io).

## Running lint

Run `nx lint redux-thunk-store` to execute the lint via [ESLint](https://eslint.org/).
